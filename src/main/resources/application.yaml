spring:
  application:
    name: backend
  datasource:
    url: jdbc:mysql://localhost:3306/namzster?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Seoul
    username: root
    password: 1234
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
    locations: classpath:db/migration